# Frontend - SenseControl

Ainda não implementado. Será o dashboard web em React/TypeScript/Vite, consumindo a API REST e o WebSocket do backend para exibir consumo atual e histórico de água/energia, alertas e recomendações.

Chega na **Etapa 4** do roadmap, depois que a API de leituras (Etapa 2) e o simulador IoT (Etapa 3) já estiverem publicando dados reais para exibir. Veja `docs/ROADMAP.md` na raiz do projeto para o plano completo.
